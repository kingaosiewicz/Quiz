<UserControl x:Class="Quiz.View.QuizEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quiz.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBox Text="{Binding QuizName}" Margin="5"/>
        <Button Content="Zapisz quiz" Command="{Binding SaveCommand}" Margin="5"/>
        <Button Content="Wczytaj quiz" Command="{Binding LoadCommand}" Margin="5"/>
    </StackPanel>
</UserControl>
