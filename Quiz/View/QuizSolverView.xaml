<UserControl x:Class="Quiz.View.QuizSolverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quiz.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <TextBlock Text="{Binding CurrentQuestion.Text}" FontWeight="Bold"/>
        <ListBox ItemsSource="{Binding CurrentQuestion.Answers}" DisplayMemberPath="Text"
                 SelectedIndex="{Binding SelectedAnswerIndex, Mode=TwoWay}" />
        <Button Content="Sprawdź" />
        <TextBlock Text="{Binding Result}" Foreground="Green"/>
    </StackPanel>
</UserControl>
